name: Tests

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  tests:
    name: PHP ${{ matrix.php }} - ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    
    strategy:
      fail-fast: true
      matrix:
        os: [ubuntu-latest]
        php: ['8.3']
        # Temporarily reduced to save CI minutes while debugging
        # Full matrix: os: [ubuntu-latest, windows-latest], php: ['8.1', '8.2', '8.3']
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          extensions: mbstring, json
          coverage: none
      
      - name: Get Composer cache directory
        id: composer-cache
        run: echo "dir=$(composer config cache-files-dir)" >> $GITHUB_OUTPUT
        shell: bash
      
      - name: Cache Composer dependencies
        uses: actions/cache@v3
        with:
          path: ${{ steps.composer-cache.outputs.dir }}
          key: ${{ runner.os }}-composer-${{ hashFiles('**/composer.lock') }}
          restore-keys: ${{ runner.os }}-composer-
      
      - name: Install dependencies
        run: composer install --prefer-dist --no-progress --no-interaction
      
      - name: Dump autoload
        run: composer dump-autoload
      
      - name: Debug - Verify test directory
        run: |
          pwd
          ls -la tests/ || dir tests
          find tests -name "*Test.php" -type f | head -10 || echo "Find command not available"
      
      - name: Debug - Verify Pest
        run: |
          vendor/bin/pest --version
          vendor/bin/pest --list-tests || echo "List tests failed"
      
      - name: Run code style checks
        run: composer test:lint
      
      - name: Run static analysis
        run: composer test:types
      
      - name: Run unit tests
        run: composer test:unit
